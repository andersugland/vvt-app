<script>
	// export let portableText;

	// $: ({ index, blocks, value } = portableText);
	// $: ({ style } = block);

	// $: ({ value } = portableText);
	// console.log('list item', portableText);
</script>

<ul>
	<slot />
</ul>
